# This empty file lives at the repo root on purpose.
# It tells pytest to add the repo root to the import path, so the tests in
# tests/ can do "import model" and "import fairness_audit" no matter how pytest
# is started (plain "pytest" on GitHub, or "python -m pytest" on my laptop).
