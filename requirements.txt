# Core scientific stack
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# Plotting
matplotlib>=3.7
seaborn>=0.12

# Optional: production-grade fairness tooling.
# I implement the metrics from scratch in this repo so the maths is transparent,
# but fairlearn is what I would reach for in a real deployment.
fairlearn>=0.10
